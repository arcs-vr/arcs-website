<!doctype html>
<html lang="en_GB">
<head>
    <meta charset="UTF-8">

    <title>ARCS &verbar; {{ head_title|default('Arbitrary Remote Control System') }}</title>

    <meta
      name="description"
      content="{{ head_description|default('') }}"
    >

    <meta
      name="viewport"
      content="width=device-width, initial-scale=1"
    >
    <link rel="stylesheet" href="https://use.typekit.net/jmh1eil.css">
</head>

<body>
{% block page %}{% endblock %}
</body>
</html>
